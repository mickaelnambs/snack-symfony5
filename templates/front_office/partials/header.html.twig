<header>
	<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
		<a class="navbar-brand" href="{{ path("app_home") }}">
			<b>Snack</b>
		</a>
		<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>

		<div class="collapse navbar-collapse" id="navbarSupportedContent">
			<ul class="navbar-nav mr-auto">
				<li class="nav-item">
					<a class="nav-link" href="{{ path("app_home") }}">
						<i class="fas fa-home"></i>
						Accueil
					</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path("item_index") }}">
						<i class="fa fa-utensils"></i>
						Menus
					</a>
				</li>
			</ul>
			<ul class="navbar-nav ml-auto">
				{% if is_granted("ROLE_USER") %}
					<li class="nav-item dropdown">
						<a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
							Options
						</a>
						<div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDopdownLink">
							<a class="dropdown-item" href="{{ path("cart_show") }}">Panier</a>
							<a class="dropdown-item" href="#">Mot de passe</a>
							<a class="dropdown-item" href="{{ path("app_logout") }}">Deconnexion</a>
						</div>
					</li>
				{% else %}
					<li class="nav-item">
						<a class="nav-link" href="{{ path("app_register") }}" tabindex="-1" aria-disabled="true">
							<i class="fas fa-sign-in-alt"></i>
							Inscription</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="{{ path("app_login") }}" tabindex="-1" aria-disabled="true">
							<i class="fas fa-lock"></i>
							Connexion</a>
					</li>
				{% endif %}
			</ul>
		</div>
	</nav>
</header>
